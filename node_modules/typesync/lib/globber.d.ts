/**
 * Globber interface.
 */
export interface IGlobber {
    /**
     * Globs for a filename.
     *
     * @param root
     */
    glob(root: string, filename: string): Promise<Array<string>>;
}
/**
 * Creates a globber.
 */
export declare function createGlobber(): IGlobber;
