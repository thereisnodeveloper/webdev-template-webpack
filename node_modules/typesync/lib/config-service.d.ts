import { type ICLIArguments, type ISyncOptions } from './types';
/**
 * Config Service.
 */
export interface IConfigService {
    /**
     * Get typesync config.
     */
    readConfig(filePath: string, flags: ICLIArguments['flags']): Promise<ISyncOptions>;
}
export declare function createConfigService(): IConfigService;
