#!/usr/bin/env node
import { SUPPORTED_PACKAGE_MANAGERS } from './utils';
export interface MainOpts {
    dev?: boolean;
    prod?: boolean;
    exact?: boolean;
    packageManager?: SUPPORTED_PACKAGE_MANAGERS;
}
declare const _default: (modules: string[], { dev, prod, exact, packageManager }?: MainOpts, shouldSpin?: boolean) => Promise<void>;
export default _default;
